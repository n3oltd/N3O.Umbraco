## Linked work issue

<!-- REQUIRED on every PR — features included. Use `re n3oltd/work#NNN` (do not use `Closes`/`Fixes`/`Resolves`). A PR with no work issue should be the rare exception, not the norm. -->

re n3oltd/work#

## Summary

<!-- Describe what changed and why, in prose. This is the source release notes are generated from — write it for a reader, not just a reviewer. Do not write release notes yourself. -->

## Test plan

- [ ]
