# N3O.Umbraco → Umbraco 17.3.5 / .NET 10 — PR Tracker

> ✅ **CURRENT STATE (updated 2026-07-22) — authoritative; supersedes the status tables below.** The live single-list status is **GitHub issue [n3oltd/work#729](https://github.com/n3oltd/work/issues/729)** (kept current there). Audit basis: PR list + full-solution build on `v17-Talha`.
>
> **Merged into `v17`** (in addition to #850 Extensions+CMS and #852 csproj-normalization already recorded below): **#854** Data (+ Data.PerplexBlocks) · **#857** UIBuilder (+ StaticAssets) · **#858** small-projects batch (Blog, Cdn ×2, Email ×4, Events, Marketing ×2, Markup ×2, Storage ×2, Sync ×3, Vacancies, Validation) · **#863** ReactRuntime (auth-fetch + WorkspaceVisibility carrier) · **#876** Authentication + Auth0 · **#877** Blazor + Blazor.BackOffice **removed** (not migrated) · **#879** Giving (+ Allocations) · **#855** Cloud.Platforms (+ Platforms.StaticAssets + Marketing.StaticAssets) *(merged 2026-07-16)* · **#856** Scheduler (+ StaticAssets) *(merged 2026-07-01)* · **#883** Auth0.ManagementApi v8.5.0 SDK migration *(merged 2026-07-01)* · **#885** dictionary placeholder validation *(merged 2026-07-01)* · **#905** v13→v17 forward-port (Group A + #904 day-of-week/month filter) *(merged 2026-07-22)* · **#926** DemoSite host (Web + Core) — the closing host *(merged 2026-08-07)* · **#927** post-DemoSite increments *(merged 2026-08-10: `BlogPost.generated.cs` raw-GUID fix + retire `N3O.Umbraco.Bundling` + `Extensions`/`Storage.Azure`/`Accounts`/`Scheduler` sync)* · **#928** WelcomeDashboard · **#929** TextResourceEditor · **#930** SerpEditor · **#931** Cells · **#932** EditorJs — the 5 backoffice plugins migrated to the `frontend/<app>` layout *(all squash-merged 2026-08-17; EditorJs now hosts EditorJS in a same-origin iframe; `UmbPropertyValueChangeEvent`→`UmbChangeEvent`)*. The csproj-normalised, code-complete-on-merge set (Captcha, Forms, KeyVault, Maps, Mediator, MessageBus, Monitoring, Redirects, Search, Telemetry, Video) is on `v17` via #852.
>
> **Forward-ports from `main` (2026-08-18):** `origin/main` is 117 commits ahead of v17 since merge-base `8f37f1dd8`; a **bulk `git merge` was attempted and found UNVIABLE** (auto-merges 126 v13-API files + resurrects deleted Cropper/Uploader — see SESSION_HANDOFF). Doing it **selectively** instead. **5 forward-port PRs open (base `v17-Talha`, each 0-error build):** **#942** scheduler worker-count (#908 finish) · **#943** analytics HTML-encode + no-store (security, #6b4c50de3) · **#944** SubscriptionId rename + webhook Code (#922) · **#945** CampaignOfferingVisibility (#203) · **#946** canonical/og/JSON-LD fixes (#917, fixes only). **Deferred (NEEDS-DECISION):** #936 fluent designers, #921 culture accessor, #924/#925 storage, CDN trio #937/#934/#902, Connect-client regen (needs live backend), Maps.Google, new SEO features. Most other main commits are already forward-ported or superseded on `v17-Talha`.
>
> **Forward-ports round 2 (2026-08-19):** #942–#946 **merged** into `v17-Talha` (tip `435a543ee`). The deferred NEEDS-DECISION batch was then done — **6 more PRs open (0-error builds, not runtime-verified):** **#947** workload-identity storage (#924/#925) · **#948** CDN trio #902/#934/#937 · **#949** culture accessor (#921) · **#950** multi-sitemap/GEO/robots + #917 leftovers · **#951** fluent CT/DT designers (#936, ADAPTED — ImageCropper/BlockGrid/RichText designers omitted, not portable to v17) · **#952** crowdfunder node + regen clients (#940/#941, **STACKED on #951 — merge after it**). All base `v17-Talha` except #952 (base `fwd-fluent-designers`). Merge order: #947–#951 any order, then #951→#952. Still deferred: standalone Connect regen, Maps.Google product call, CI/chore batch (10× `.gitignore` + bots + PR template) → final solution-wide PR.
>
> **v13→v17 forward-port — ✅ done (#905, merged 2026-07-22):** all of `V13_FORWARD_PORT.md` "Group A" + v13 commit `104c48673` (#904). Note #905's Cloud/Cloud.Platforms/Search.Typesense parts only build once those projects' pending net10 migration lands on `v17` (pre-existing v17 gap); verified 0 errors on `v17-Talha`. Open runtime step: offering doctype must carry the 3 embed-code property aliases.
>
> **Open PRs into `v17`:** **#845** `v17-Talha` umbrella · **#974** `v17-stj-and-block-preview` (block preview batching + System.Text.Json seams + `App_Plugins` revalidation — see the table below) — **DRAFT** as of 2026-08-31, awaiting un-draft. *(2026-07-22: #855 Cloud.Platforms, #856 Scheduler, and #905 forward-port have now merged — moved to the merged list above.)*
>
> **Linked PR in the sites repo:** **n3oltd/sites#154** removes one site's `ConfigureStaticFiles` override
> (`ServeUnknownFileTypes`), which #974 would otherwise switch on for the first time at that site's v17
> upgrade. Base `main`, zero-regression, independent of #974's merge order.
>
> ⚠️ **#974 overlaps #845.** Its commits also sit on `v17-Talha` locally but were **deliberately not pushed**
> there, so they do not appear in #845 twice. If `v17-Talha` is pushed before #974 merges, both PRs will
> contain the same commits.
>
> ✅ **RECONCILED 2026-07-28 — supersedes the older "not yet sliced" list below.** Every area on `v17-Talha` was diffed against `origin/v17`; **most of the previously-listed pending set is already on `v17`**.
> - **Already merged to `v17` (0 diff, nothing to PR):** Analytics, Blocks (×3, via #906/#909/#910), Forex (×3), GeoIP (×2), **Newsletters (×3)**, Payments (×9), TaxRelief (×2), Templates (×3), Workflows (×2).
> - **Genuinely still pending (`v17-Talha` → `v17`):**
>   - **Plugins** — ✅ **5 backoffice plugins MERGED to `v17` via #928–#932 (2026-08-17):** Cells, EditorJs, SerpEditor, TextResourceEditor, WelcomeDashboard, all on the `frontend/<app>` layout. **Cropper + Uploader removal → now PR [#939](https://github.com/n3oltd/umbraco-extensions/pull/939)** (2026-08-18, base `v17`; deletes both plugin projects incl. `.Data`/`.StaticAssets`, the two `N3O.Umbraco.Clients` clients + DI, and repoints `ImageProcessing` off Uploader → `N3O.Umbraco.Plugins`). **⚠️ Breaking for 17 sites / 334 generated models** that embed `N3O.Umbraco.Cropper.Models` — Talha confirmed the sites move to native `MediaWithCrops` on their own v17 upgrade. `.sln` entries stay for the final solution-wide PR. Sync-back merge `57cea1496` pulled the plugins' post-merge review enhancements back onto `v17-Talha`; orphan `.d.ts` shims dropped (`c8fbabebf`).
>   - ~~**DemoSite** (Web + Core) — the closing host (177 new / 72 modified).~~ ✅ **MERGED to `v17` via #926 (2026-08-07);** the broken `BlogPost.generated.cs` it shipped was **fixed via #927 (merged 2026-08-10)**.
>   - **Bundling — ❌ RETIRED. ✅ removal merged to `v17` via #927 (2026-08-10).** PR #915 (keep-Smidge) closed by nadrummond; the CMS-side Smidge seam does not survive the migration. `N3O.Umbraco.Bundling` (`src/Bundling/**` + `.sln` entries) is now deleted on both `v17` and `v17-Talha`. The **5 consuming sites** (mth/mr/mpb/afh/mh — `<n3o-css-bundle>`/`<n3o-js-bundle>` + `OurSmidgeOptions`) must **bundle at source with Vite** in their own repos — post-migration follow-up. Tracked in n3oltd/work#3211.
>   - **Post-merge increments to already-merged shared projects.** ✅ **Shipped via #927 (merged 2026-08-10):** `N3O.Umbraco.Extensions`, `Accounts`, `Storage.Azure`, `Scheduler` (SchedulerSettings + OpenIddict 7.2.0 pin), `N3O.Umbraco.sln` (Bundling removal). **Sliced 2026-08-18:** Data (5 M) → PR [#938](https://github.com/n3oltd/umbraco-extensions/pull/938); Clients (Cropper/Uploader client removal) + ImageProcessing → PR [#939](https://github.com/n3oltd/umbraco-extensions/pull/939). **Search/Monitoring/Cloud/Sync/Cdn — already on `v17` (0 diff)**; the 2026-08-18 reconciliation confirmed the entire remaining `origin/v17 → v17-Talha` diff was only Cropper/Uploader + Data + Clients + ImageProcessing (+ the by-design docs and `.sln`). **After #938 + #939 merge, only the final solution-wide PR remains** (`.sln`, `Directory.Build.*`, root `*.md` docs, `docs/frontend/**`, `inspect_usync.csx`).
>   - ✅ **Post-DemoSite increments — PR #927 MERGED 2026-08-10 (`52f3acf4e`)** (branch `v17-post-demosite-increments`, base `v17`): BlogPost fix + Bundling retirement + Extensions/Storage.Azure/Accounts/Scheduler sync. Build-verified 0 errors (Extensions/Accounts/Storage.Azure/Scheduler); `DemoSite.Core` not fully built (pre-existing net8 Plugins gap). On sync-back, the 5 files excluded from #927 (2 comments + HealthCheck/Localization/Notifications) were aligned to `v17` so `Extensions` is now identical on both branches.
>   - **Blocks review follow-ups** — draft PR **#914** (Perplex 4.0.3 + ContentHelper header/settingsData/alias/JSON) — see Backlog.
> - **Lives on `v17-Talha` by design (not merged to `v17`):** the root migration `*.md` docs, `docs/frontend/**` (29), and `inspect_usync.csx`.
>
> **⚠️ Build-state correction (updated 2026-07-22):** the "0 errors" claims below are stale. **Two of the three prior root causes are now resolved:** the Auth0 SDK bump (#883 merged) and the #856 gating (Scheduler is now net10 on `v17`, so `N3O.Umbraco.Data`'s reference no longer forces a net8 restore). Full `dotnet build N3O.Umbraco.sln` on `v17-Talha` now fails from **2 remaining causes**: (1) `npm ci` env failure in `Directory.Build.targets` (exit -4048); (3) `DemoSite.Web` stale `Import` of `N3O.Umbraco.Cms\build\N3O.Umbraco.Cms.targets` (Cms is now an RCL). Every other project compiles.
>
> ⚠️ **CURRENT STATE (2026-06-24):** `origin/v17` was merged into `v17-Talha`, **RESTRUCTURING** the frontend/build layout. The authoritative current layout is in **AGENTS.md**. Dated PR rows / notes below referencing `ClientApp/`, `Apps/`, `Cms/Build/`, `Cms/Extensions/`, `@n3o/build`, `@n3o/auth-fetch`, `@n3o/backoffice-core`, `N3O.Umbraco.React`, `BuildClientApp`/`BuildReactRuntime`, or `KonstruktConfigurator` reflect earlier intermediate states and are retained as history. **Current names:** shared packages `@repo/build-config` + `@n3oltd/backoffice-core` + `@n3oltd/backoffice-ui` under `src/frontend/`; React runtime is the standalone `N3O.Umbraco.ReactRuntime` project; per-plugin apps at `<Project>/frontend/<app>/`; build via root `Directory.Build.props`/`.targets` + Turbo; `KonstruktConfigurator`→`UIBuilderConfigurator`. **NOTE:** an old parallel layout (`Cms/Build/*`, `Cms/Extensions/*`, `Data.StaticAssets/Extensions/*`) is still git-tracked pending deletion.

> Status tracker for the GitHub migration issue. **"Done" = the work is in a branch that has an *active PR*.** Everything else is **in progress** — including work that's already pushed to a branch but does **not** yet have a PR open.
>
> The migration is shipped as **one PR per project** (base = `v17`), the same way `N3O.Umbraco.Extensions` was. The integration branch `v17-Talha` (and its backup) is **not** a tracked PR — it's the working branch the per-project slices are cut from. Repo: `github.com/n3oltd/umbraco-extensions`.

Legend: ✅ Done (in an active PR) · 🔄 In progress (pushed, no PR yet) · ⏳ Not started / deferred

---

## ✅ Done — in an active PR

| Workstream | Branch | PR | Base | Contains |
|---|---|---|---|---|
| **Extensions + CMS** | `v17-N3O.Umbraco.Extensions` | **#850** ✅ **MERGED 2026-06-11** (branch deleted on origin) | `v17` | Core framework migration (Extensions + Cms); the shared **React** runtime (`N3O.Umbraco.ReactRuntime`: react / react-dom / jsx-runtime); v17 **Block List/Grid data export** fix (`ContentHelper`); DynamicListViews + its `{contentId:guid}` visibility route. |
| **csproj normalization** | `v17-csproj-normalization` | **#852** | `main` | net10 / v17 `.csproj` normalization across 70 leaf projects (cross-cutting chore, separate from the per-project v17 PRs). |
| **Block preview + STJ seams** | `v17-stj-and-block-preview` | **#974** (opened 2026-08-31) | `v17` | Four independent v17 correctness fixes, cut from `origin/v17` and cherry-picked (18 files, +629/-294): (1) **Blocks** — block preview batched to one request per document instead of one per block, request body read via `IJsonSerializer` (fixes a 500 from binding `BlockValue.Layout`, an interface element type), custom view renders `umb-block-grid-areas-container` so sections can be filled, and a section previews its **shell only** so its children are not drawn twice; `ContentHelper` threads the property `owner` to the value converters (required by block editors since v15). (2) **Newtonsoft→STJ** — five value converters `is JObject` → `is JsonNode`. *(Corrected 2026-08-31: the branch is **unreachable** under v17 — `JsonObjectConverter` never produces a `JObject` — but no caller was shown to reach it, since a stored value arrives as a string and hits the untouched `source is string` branch. Treat this as replacing a dead branch, NOT as fixing an observed null return; the earlier wording here and in the PR body overstated it.)* `BlockListPropertyBuilder` serializes via `IJsonSerializer`. (3) **Platforms.Marketing** — telethon rule bundle no longer exports a non-manifest descriptor (`Extension is missing alias` ×2 on every backoffice load). (4) **Cms** — `UseStaticFiles` now receives the configured `StaticFileOptions` (every `ConfigureStaticFiles` override was silently inert, incl. one site's `ServeUnknownFileTypes`) and `/App_Plugins` responses require revalidation. **Note:** this bundles four areas rather than following one-PR-per-project.<br><br>**2026-08-31 — two further commits after an adversarial review:** `570f038d0` fixes 8 findings (coordinator batch serialisation, stale-fingerprint clear on instance change, null auth-fetch guard, pending-set clear ordering, re-register on reconnect so sorting cannot orphan a block; restored scalar-array and multiple-textstring handling; `UdiParser.TryParse` instead of throwing `Udi.Create`; typed `PreviewBlocksRes` with a `failed` list) and `6700da934` trims comments that restated the code. Also disclosed: the rewrite **fixes a stale static element-type cache** that made newly-created element types invisible until app restart, and `ToEditorData` now **mutates its input** where the old code copied first. **Still unverified:** the culture-varying `owner`/`Expose` path — runtime testing was on an invariant document. |

> **⚠️ #850 is merged — no live Extensions branch.** Post-#850 increments to `N3O.Umbraco.Extensions` / `N3O.Umbraco.Cms` (e.g. the 2026-06-16 audit + the typed workspace-visibility refactor) therefore have **no dedicated Extensions branch** to land on. They live on `v17-Talha` and ride to `v17` via the **`v17-N3O.Umbraco.ReactRuntime`** branch (full-repo; carries the Cms changes + the Extensions visibility files + repo-root `Directory.Build.targets`) and/or the eventual solution-wide PR. **Do not recreate `v17-N3O.Umbraco.Extensions`** (a stray recreation on 2026-06-16 was deleted).

**Resumed (2026-06-12):** the shared `@n3o/auth-fetch` runtime + `N3O.Condition.WorkspaceVisibility` condition + adoption README were deferred out of `#850`; they have now been shipped as a sequenced set of branches (see "Auth + visibility runtime" below). The ReactRuntime branch must merge into `v17` **before** the four consumer branches.

---

## 🔄 In progress — pushed to a branch, **no PR open yet** (ready to raise)

Each is built, committed, and pushed; just needs a PR opened against `v17` (compare URL: `…/compare/v17...<branch>`).

> **Branch convention (2026-06-13):** each domain project and its `.StaticAssets` backoffice sibling now live on **one combined branch** named after the domain project (e.g. `v17-N3O.Umbraco.Data` carries both `N3O.Umbraco.Data` and `N3O.Umbraco.Data.StaticAssets`). The former separate `*.StaticAssets` branches were deleted. **Keep this for all future per-project branches.** These branches contain **only the two project directories** — no solution-wide files. The npm-workspace unification (root `package.json` glob, `package-lock.json`, `tsconfig.base.json`, `build/vite-config`, `Directory.Build.targets`) + the `ClientApp→Extensions` rename is inherently solution-wide and is **deferred to the solution-wide PR** (it lives on `v17-Talha`, commit `e89ee591e`); it can't be sliced per-project because it depends on every app's migration being present.
>
> **Correction (2026-06-16):** the per-project `.StaticAssets` branches were since re-cut onto the **`Extensions/` layout** (renamed from `Apps/` to match Umbraco's "extensions" terminology) and the **`@n3o/backoffice-core`** runtime name (the earlier `ClientApp` / `@n3o/auth-fetch` wording above is obsolete). They remain **pre-_workspace-unification_**, however: no root workspace `package.json`, explicit per-app `dependencies`/`devDependencies`, and the shared Vite config imported via a relative path (`../../../build/vite-config`) rather than the `@n3o/build` package. So the full npm-workspace unification is still the only piece deferred to the solution-wide PR. **Update (2026-06-16):** `v17-N3O.Umbraco.ReactRuntime`'s entire `N3O.Umbraco.Cms` directory has since been **synced to `v17-Talha`** (so it now carries the Cms restructure — `Cms/Extensions/N3O.Umbraco.BackofficeCore`, ReactRuntime under `Cms/Build/` — plus the audit + visibility changes), and the repo-root `Directory.Build.targets` was added to it. `#850` (Extensions) is merged, so it has no live branch.

| Project (domain + .StaticAssets) | Branch | Tip | Notes |
|---|---|---|---|
| **ReactRuntime (auth + visibility)** — now the de-facto Cms+Extensions carrier post-#850 | `v17-N3O.Umbraco.ReactRuntime` | `b6eec1852` | **Foundation — must merge first.** Shared `@n3o/auth-fetch` (`createAuthFetch` + `UmbAuthFetchMixin`) + `N3O.Condition.WorkspaceVisibility` condition. **2026-06-16:** its entire `N3O.Umbraco.Cms` was **synced to `v17-Talha`** (Cms restructure: `Apps/N3O.Umbraco.BackofficeCore`, ReactRuntime→`Build/`; + audit changes; + the typed-visibility condition & retired bespoke DynamicListView condition; + the intentional comment-stripping of `auth-fetch.ts`/`@n3o/build` config files & deletion of `Build/N3O.Umbraco.ReactRuntime/README.md`). `Cms` is kept **identical to `v17-Talha`** (re-synced after each Cms change). Also brought: the **Extensions visibility files** (`WorkspaceVisibilityController` + `WorkspaceVisibilityRes` + `DynamicListViewApiController`) and the repo-root **`Directory.Build.targets`** (was missing). Other `N3O.Umbraco.Extensions` files remain ~11 behind (not visibility-related). **+ 2026-06-17:** added the repo-root **`src/package.json`** (npm-workspace root) to this branch; and `Directory.Build.targets` had its `Content Remove` package.json/tsconfig `ItemGroup` dropped and its explanatory comments stripped (both also done on `v17-Talha`). **+ 2026-06-19 (`b6eec1852`):** Cms `Apps`→`Extensions` rename (Cms re-synced to `v17-Talha`; `Cms.csproj` → `Extensions\*\package.json`); now also carries the latest `Directory.Build.targets` + `src/package.json` and **`src/package-lock.json` (newly added)**. |
| **Data** (+ Data.StaticAssets) | `v17-N3O.Umbraco.Data` | `ab81c5702` | Combined. v17 migration; **runtime-verified import flow** (queue → Hangfire → content created); `EnsureDataTypeExistsAsync` sets `EditorUiAlias`; import status-row DB connection isolated (DL-05); Export/Import visibility controllers + manifests restored. StaticAssets: authed Export/Import + ImportDataEditor via shared `@n3o/auth-fetch`; native HTML controls (upstream uui FormControlMixin/React bug); "View queue" → v17 route. **+ 2026-06-16 audit:** Export/Import lifecycle hardening (AbortController/generation-counter, timer cleanup, `useExportServerData`/`useImportLookups` hooks, `response.ok`) + Data.Import decomposition (ImportForm/Success/Error + types) + stable keys. **+ data-export-app.tsx decomposed** (491→161; `types.ts`/`use-export.ts`/`export-options.tsx`/`selectable-field-list.tsx`). **+ typed visibility:** Export/Import controllers now extend `WorkspaceVisibilityController` (typed `WorkspaceVisibilityRes`; no anonymous `{ permitted }`) — ⚠️ **depends on `WorkspaceVisibilityController`/`Res` from Extensions reaching `v17`** (see #850 note above). |
| **Cloud.Platforms** (+ Marketing) | `v17-N3O.Umbraco.Cloud.Platforms` | `358c4e9dd` | **2026-07-01 (debate-consensus refactors + embed codes):** applied 8 subtractive refactors from a multi-agent design review — `GetContentUrls` extracted to a mediator `GetContentUrlsQuery`/handler (thin controller); `ContentUrlsRes` dropped the redundant `Permitted`; content URLs now gated on `IsPublished`; `GetPreviewHtmlHandler` drops redundant `IContentTypeService`; **Preview app ported React→Lit** (`platforms-preview` = one `UmbLitElement`, no react); **Preview visibility** switched from `PlatformsPreviewVisibilityController` + `N3O.Condition.WorkspaceVisibility` to the built-in `Umb.Condition.WorkspaceContentTypeAlias` (`match: platformsOffering`; controller **deleted**); dead comment-only `CampaignSending`/`OfferingSending` **deleted**. **Embed codes restored:** new `CampaignEmbedCodes` handler writes the `<n3o-donation-button/form/popup>` snippets into the read-only embed properties on `ContentPublishingNotification` and clears them on `ContentUnpublishedNotification` — **runtime-verified on the test site** (publish sets, unpublish clears). Cloud.Platforms builds **0 errors**. _Supersedes the 2026-06-30 notes below re: `*Sending` comment stubs, `PlatformsPreviewVisibilityController`, and React Preview._ <br> **2026-06-30: re-cut off `v17` to the new `frontend/<app>` layout** — Preview = React `platforms-preview`; Platforms URLs `workspaceInfoApp` **split** into its own Lit `platforms-urls` package (`App_Plugins/N3O.Umbraco.Cloud.Platforms.Urls`); telethon → Lit `telethon-on-air-rule`; both `*.StaticAssets` projects **deleted** (RCL static assets in the domain projects); Preview visibility → shared `WorkspaceVisibilityController` base (`PlatformsPreviewVisibilityController`). _History (old `127cff25f` layout):_ Combined. v17 migration; Preview visibility endpoint restored. **BLOCKER-04 resolved:** TelethonOnAir registered via `engageSegmentRule` extension type (Engage v17 API confirmed from `manifests-personalization.js`; verified live in private manifest + Extension Insights). Preview `contentTypeAlias` undefined fix (route now accepts GUID, resolves alias server-side). `CampaignSending`/`OfferingSending` comment-updated (embed codes work; URL display + tab visibility deferred). Auth0 `UserSaving` + `ExceptionMiddleware` null guards. Marketing.StaticAssets telethon-on-air-rule App_Plugins added to this branch. **+ 2026-06-16 audit:** preview raw `fetch()` ×2 → `authFetch` (shell-supplied token); leaked inner `setInterval` fixed; `uui-react.d.ts` trimmed to display-only; stale TODO removed. **+ typed visibility:** PlatformsPreview visibility endpoint returns typed `WorkspaceVisibilityRes` (no anonymous `{ permitted }`) — ⚠️ depends on `WorkspaceVisibilityRes` from Extensions reaching `v17`. |
| **Blocks** | `v17-N3O.Umbraco.Blocks` | `5601dcb1f` | v17 migration; dead `BlockItemDataExtensions` removed. _(Predates the workspace restructure — will need the same re-cut treatment.)_ **+ 2026-06-16 audit:** `block-preview` raw `fetch()` → `UmbAuthFetchMixin`/`authFetch` (the `blockPreviewBackoffice` endpoint is `[Authorize]`); `uui-react.d.ts` trimmed to display-only. **+ 2026-07-24:** Perplex-v4 `Invalid content key` workaround for Image Cropper/File Upload nested in blocks (`Blocks.Perplex/PropertyEditor/` + composer `Exclude<PerplexContentBlocksPropertyEditor>`; build 0 errors); reported upstream (#102 / PR #103 — remove workaround when it ships); Media Picker not affected. Branch still needs the clean re-cut off current `origin/v17`. |
| **Scheduler** (+ Scheduler.StaticAssets) | `v17-N3O.Umbraco.Scheduler` | `d1bc81f2c` | Combined. v17 migration. **+ 2026-06-16 audit:** dashboard CSS → shadow-root `adoptedStyleSheets`. **+ 2026-06-17:** `Extensions/package.json` version normalized to `1.0.0` (reverted the earlier 17.0.0 alignment — the private, non-published client-app workspace members all sit at 1.0.0). **NOTE:** the audit's `.csproj` inline-build-target removal was **NOT** applied — it depends on the generalized `BuildClientApp` target in the solution-wide `Directory.Build.targets`, absent on this branch (which still relies on its inline `BuildClientApps` target). |

> **`Apps` → `Extensions` rename (2026-06-19) — committed + pushed to every affected branch.** The backoffice frontend source folders were renamed `Apps` → `Extensions` (Umbraco terminology). Each per-project branch carries **only its own project's** folder rename (minimal delta): Data `ab81c5702`, Cloud.Platforms `127cff25f`, Scheduler `d1bc81f2c` (also repointed its inline `BuildClientApps` csproj target), Blocks `5601dcb1f` (still stale — needs the re-cut). The Cms rename + the solution-wide wiring (`src/package.json` glob `**/Extensions/**`, `Directory.Build.targets`, `Cms.csproj`, regenerated `package-lock.json`) live on `v17-Talha` `a81c7180b` (+ backup mirror) and ride to `v17` via **ReactRuntime** `b6eec1852` (which now also adds `src/package-lock.json`). The 6 unsliced plugins' renames are on `v17-Talha`/backup only.
>
> **Merge order:** `v17-N3O.Umbraco.ReactRuntime` → `v17` first; then `v17-N3O.Umbraco.Data` / `v17-N3O.Umbraco.Cloud.Platforms` (they `import '@n3o/auth-fetch'` + reference the condition, which only exist once the runtime lands). The `Apps`→`Extensions` rename reinforces this: the per-project branches now use `Extensions/` folders while the matching `Directory.Build.targets`/`Cms.csproj` wiring reaches `v17` only via ReactRuntime.
>
> **DynamicListView endpoint lock-down (2026-06-16) — held for the Extensions PR:** the audit hardened `DynamicListViewApiController` (`ApiController` → `BackofficeAuthorizedApiController`, route → `/umbraco/backoffice/api/...`, `[Authorize]`) and switched `dynamic-list-view-condition.ts` to `createAuthFetch`. This is on `v17-Talha` but was **NOT** applied to `v17-N3O.Umbraco.Extensions` (#850, active PR): the condition now depends on the `@n3o/backoffice-core` auth runtime, which is **not on that branch**, and the controller route-change + condition must land together. Apply when the auth runtime is present on/under that PR (or fold into the solution-wide PR).

---

## ✅ Resumed — Auth + visibility runtime (shipped to branches 2026-06-12)

- **Shared backoffice runtime + content-app visibility gating** — `@n3o/auth-fetch` (`createAuthFetch` + `UmbAuthFetchMixin`) and the configurable `N3O.Condition.WorkspaceVisibility` condition, restoring the v13 `IContentAppFactory` per-node/per-user gating for Export / Import / Preview. Previously deferred from all per-project slices; now restored as the sequenced branch set above (`v17-N3O.Umbraco.ReactRuntime` foundation + the four consumers). No PRs raised yet; ReactRuntime merges first.

## ⏳ In progress — remaining known issues (Data / Data.StaticAssets)

- ✅ **TextResourceEditor** — native-control rework done (2026-06-15, session 17): `<uui-input>` → native `<input>` (React `onChange`), the last plugin hit by the upstream uui FormControlMixin+React bug. On `v17-Talha` (not yet sliced into a per-project branch).
- Block List lookup schema (`PropertyType.Nested`) needs real-content validation.
- `ImportsMigrationsComponent` plan name not namespaced (rename has migration-rerun risk — needs a decision).
- Export memory (`ProcessExportHandler` buffers whole file) — no safe in-scope fix; needs a multi-project streaming change.
- ✅ **CS0618 `IDataTypeService.GetDataType(int)`** — replaced with `GetAsync(propertyType.DataTypeKey)` (2026-06-14).
- ✅ **CS0618 `IAuditService.GetLogs(int)`** — replaced with `GetItemsByEntityAsync(...).Items.FirstOrDefault()` (2026-06-14).
- `EPPlus 8.6.0` (tracker previously said 8.5.4) commercial-license review. **⚠️ Compliance:** `DataComposer.cs:28` currently asserts `SetNonCommercialOrganization("N3O")`, which does NOT cover for-profit company use — non-compliant as-is. Footprint is tiny (1 project, 4 files, export-only). Fix = buy a commercial license (one-line) **or** swap to ClosedXML/MIT (~1–5 days). See [`BACKLOG_SCOPING.md`](BACKLOG_SCOPING.md) §6.

## ⏳ In progress — remaining known issues (Cloud.Platforms)

- ✅ **Campaign/Offering URL display** — resolved 2026-06-14 (session 16). New `N3O.WorkspaceView.PlatformsUrls` workspace view: `PlatformsBackOfficeController.GetContentUrls` endpoint + `platforms-urls.ts`/`platforms-urls-app.tsx`; shows staging + production URLs for campaigns and offerings using `ContentLocatorExtensions` + `UrlSettingsContent`. Build 0 errors; frontend builds 0 errors.
- ✅ **BLOCKER-04: TelethonOnAir segment rule client-side registration** — resolved 2026-06-14. `engageSegmentRule` extension type confirmed from `Umbraco.Engage.StaticAssets` 17.2.2; registered in `umbraco-package.json`. Verified live in private manifest + Extension Insights with Engage running.
- ✅ **Cleanup** — resolved 2026-06-14 (session 16). Deleted `PlatformsContentAppsComposer.cs` + `PlatformsPreviewApp.cs` stubs; removed stale `TODO Migration Review` from `LinkExtensions.cs`; removed `SubscriptionFile.cs` TODO comment.
- Crowdfunding tab visibility for unpublished campaigns — needs a workspace condition (deferred).

## 🔄 In progress — pushed to a branch, no PR yet (small-change projects, session 16)

| Branch | Tip | Projects | Notes |
|---|---|---|---|
| **`v17-small-projects`** | `6d899eaeb` | Authentication + Auth0, Storage + Storage.Azure, Blog, Events, Vacancies, Email, Validation, Cdn.Cloudflare, Sync.Extensions, Bundling, Marketing, Markup.Markdown | 14 projects · 33 files · each ≤3 non-csproj changes |

## ⏳ Not started — not yet sliced into per-project PRs

- **All other framework projects** (e.g. Giving.*, Crowdfunding, Forms, Maps.Google, Webhooks, Blazor.BackOffice, Plugins, Search, etc.) — still only in `v17-Talha`, not yet cut into per-project branches/PRs. _(2026-06-16: `Scheduler` is now in the pushed-branch table above; `v17-N3O.Umbraco.UIBuilder` also exists as a pushed branch — contents not yet logged here.)_ **NOTE:** the 2026-06-16 audit also touched several still-unsliced backoffice plugins (SerpEditor, Cells, EditorJs, TextResourceEditor, WelcomeDashboard, Blazor.BackOffice) — those audit changes currently live **only on `v17-Talha`**.
- **DemoSite** (Web + Core, the test harness) and **solution-wide files** (`Directory.Build.props/targets`, `*.sln`, `NuGet.Config`, root `.gitignore`, `.github/` CI, root migration `*.md` docs) — deferred to a single PR **at the end**, per the agreed workflow.
- ✅ **Superseded/Done 2026-06-24 (replaced by `src/frontend/` workspace).** **Backoffice npm-workspace unification + `ClientApp→Extensions` rename** — solution-wide (root `package.json` glob + `package-lock.json` + `tsconfig.base.json` + `build/vite-config` + `Directory.Build.targets` + the 12 folder renames). Lives on `v17-Talha` (`e89ee591e`); part of the final solution-wide PR. Can't be sliced per-project (depends on all app migrations being present). _(2026-06-16: the `ClientApp→Extensions` rename and the `@n3o/backoffice-core` naming have **already** been propagated to the per-project `.StaticAssets` branches; what remains solution-wide is the npm-workspace unification itself — root workspace `package.json`/lockfile, `tsconfig.base.json`, the `@n3o/build` package, and `Directory.Build.targets` — so those branches still carry explicit per-app deps + a relative `build/vite-config` import.)_

## ⏳ Backlog — remaining migration work (pending / not yet scheduled)

> **v13 → v17 forward-port (added 2026-07-22):** fixes/changes that landed on the **v13 line (`origin/main`)** after the migration branched (base `8f37f1dd8`) and need forward-porting into v17 — **19 logical items** (9 critical standalone fixes, 3 features, a client-regen cluster, 3 decisions). Full itemized list with commit hashes, v17 status, port locations and gotchas: **[`V13_FORWARD_PORT.md`](V13_FORWARD_PORT.md)**. Report-only so far — no code ported yet.

> **Scoped (2026-06-15):** every item below was researched for achievability — scope, verdicts, file refs, and cited licensing sources are in [`BACKLOG_SCOPING.md`](BACKLOG_SCOPING.md). Corrections from that research are folded into the items below.

- **N3O Image Cropper → Umbraco Image Cropper + Uploader** — replace the custom Cropper/Uploader plugins with Umbraco's built-in image cropper + upload editors.
- **Perplex Content Blocks — ✅ MERGED to `v17`** via **#906** (migration, Perplex 3.0.1→4.0.0) + **#909** (block-preview iframe fixes) + **#910** (Perplex head/startup/version-pin fixes); now on `v17-Talha` too (merge `1bf3b4412`). **Bumped `4.0.2` → `4.0.3` (2026-07-27, `Blocks.Perplex.csproj`).** 4.0.3 ships the upstream fix for the "Invalid content key" cropper/File-Upload-in-a-block bug (Perplex **#102**, via Talha's PR **#103**, both merged 2026-07-27). ⚠️ Bump not build-verified here (`v17` doesn't restore end-to-end; can't restore locally).
  - **✅ Workaround REMOVED (2026-07-27, on `v17-Talha`, uncommitted):** deleted `Blocks.Perplex/PropertyEditor/N3OContentBlocksValueEditor.cs` + `N3OContentBlocksPropertyEditor.cs`, and the `Exclude<PerplexContentBlocksPropertyEditor>()` line (+ 2 orphaned usings) in `PerplexBlocksComposer` — Perplex's own (4.0.3-fixed) editor now handles the key-threading. `dotnet build Blocks.Perplex` = **0 errors**. **⚠️ Owed: backoffice retest** — Image Cropper + File Upload inside a block still save/persist on 4.0.3 with the workaround gone.
- **Blocks review follow-ups Noor flagged (#906/#909/#910).** Several now **✅ done in code on `v17-Talha` (2026-07-27, uncommitted; `dotnet build` 0 errors — runtime/backoffice verification still owed, Part 6)**; the rest flagged below. **When cutting the next Blocks / `ContentHelper` PR, flag that Noor wanted these done.** Detail: `C:\Users\TalhaMalik\Downloads\message-for-talha (1).md` + `SESSION_HANDOFF.md` (2026-07-27 entry).
  - **✅ n3oltd/work#3156** — `ContentHelper` now parses the Perplex **header** block (`GetContentPropertiesForBlockContent` iterates `header` as well as `blocks`). *(close after runtime verify)*
  - **✅ n3oltd/work#3157** — `ContentHelper` now parses block **`settingsData`** (native BlockList/Grid path reads `settingsData` as well as `contentData`, via new `AddBlockListOrGridElements`). *(close after runtime verify)*
  - **✅ v3 case-sensitive alias** — `GetContentPropertiesForNestedContentElement` now resolves aliases case-insensitively (matches the v4 path; the prod `"PropType"` value is no longer dropped).
  - **✅ `ElementsProperty.Json` re-serialisation** — `GetJsonPropertyValue` now returns the stored JSON string verbatim (no Newtonsoft round-trip → no date-TZ coercion / number-precision loss).
  - **✅ Part 5 — coupling documented.** Kept `AddMvcCore().AddRazorRuntimeCompilation()` **removed** (re-adding = a 2nd MVC builder alongside Umbraco's `AddControllersWithViews()` = the v17 startup conflict); added a comment in `BlocksComposer.ConfigureRazorTemplating` noting runtime Razor compilation is enabled by the host — `N3O.Umbraco.Cms` (`CmsStartup.cs:51`), which every N3O site installs. ⚠️ .NET 10 marks `MvcRazorRuntimeCompilationOptions` obsolete (ASPDEPR003) — the preview's runtime-Razor approach needs a longer-term rethink.
  - **⏳ `culture`/`segment` fold — PROPOSED, not done (needs a decision):** `GetBlockElementValuesByAlias` folds `values[]` on alias alone, so the last entry wins for a variant element regardless of requested culture. Fix = thread the requested `culture` from `GetContentProperties(IContent, culture)` down through the block parsers (a signature cascade) + decide the fallback (culture match → invariant → first). Noor only *noted* this — held for sign-off.
  - **⏭️ `CompositionPropertyTypes` cache — deferred, logged as a come-back-to note on [#729](https://github.com/n3oltd/work/issues/729#issuecomment-5102748552)** (premature optimisation; Noor flagged it only "if export gets slow" — no perf problem demonstrated). **Verified against 17.3.5 source** (`ContentTypeCompositionBase.cs`, `release-17.3.5`): Umbraco intentionally does **not** cache it — the getter `DeepClone`s every composed property on each enumeration (recursively through nested compositions), and the code comment states a cache would miss composition changes. So any cache must be **per-call in `ContentHelper`** (memoise per content-type for one export/parse), not expected from Umbraco.
  - **Behaviour changes to watch (first save after deploy):** composed properties are now actually validated/cropped (2.1); two blocks of the same element type now share a media directory (3.1); `ContentBlocksPropertyBuilder` ctor gained a public `IJsonSerializer` param (packable — downstream constructing it directly must update).
  - **Manual verification (Part 6 — CI can't):** drive a real backoffice (no `Failed to resolve module specifier`, JS blocks animate, **clicking a block still opens it**); Image Cropper + File Upload inside a block, then on a **multi-culture** node (also covers the #3156 / #3157 / alias / JSON-bytes changes **and** the Perplex 4.0.3 workaround removal); restore a real v13 DB + upgrade (Perplex migration-plan `umbracoKeyValue` row must exist, else blocks silently vanish; migrates only the latest 8 versions); build `site-mh` against the packed packages (`IBlockPreviewer` signature changed, no in-repo impl); Settings→Data Types shows ~134 orphaned `Umbraco.NestedContent` data types on upgraded sites.
- **JS calls + authentication header** — generalize the authenticated bearer-token fetch across all backoffice JS calls / plugins (ties into the deferred `@n3o/auth-fetch` shared runtime).
- **`.targets` in build** — remove the remaining per-project `build/*.targets`. `Blazor.BackOffice` was the last RCL-conversion leftover and is now converted (2026-06-15, session 17: `build/*.targets` deleted, assets → `wwwroot/App_Plugins/`). Only the intentional **wrapper** StaticAssets pass-throughs (`Forms`/`Maps.Google`/`Marketing`/`UIBuilder`/`Workflows`) remain to review.
- ✅ **Rename `ClientApp` → `Extensions` across all backoffice projects (done 2026-06-13; folder further renamed `Apps` → `Extensions` on 2026-06-19 to match Umbraco's "extensions" terminology).** The root `src/package.json` npm-workspaces list is now a glob (`"**/Extensions/**"`, `"N3O.Umbraco.Cms/Build/*"`, `"!**/bin/**"`, `"!**/obj/**"`) so new frontend projects are auto-discovered — the glob keys off a folder named **`Extensions`**. The 12 leaf frontend folders were `git mv`'d `ClientApp`→`Extensions` via `Apps` as an intermediate name (Blazor.BackOffice, Blocks, Cloud.Platforms, Data, Cells, Cropper, EditorJs, SerpEditor, TextResourceEditor, Uploader, WelcomeDashboard, Scheduler `.StaticAssets`). The **only** functional reference was the shared `BuildClientApp` target in repo-root `Directory.Build.targets` (`Exists(...\ClientApp\package.json)` → `...\Extensions\...`); the per-project `.csproj` files and vite/tsconfig use relative paths (same depth) so needed no change, and `N3O.Umbraco.Cms` already used `Extensions/*`. `src/package-lock.json` regenerated via `npm install`; `npm query .workspace` lists **14** (12 apps + DynamicListViews + ReactRuntime); `dotnet build N3O.Umbraco.sln` → **0 errors**. Committed on `v17-Talha` (`e89ee591e`); ships in the solution-wide PR (not the per-project branches — see the deferred list above).
- **Deprecated-dependency audit** — Newtonsoft `Json.NET` and other deprecated packages/mechanisms — find and replace. **`build.targets` sub-item is ✅ done** (zero hand-authored `build/*.targets` remain). Newtonsoft is the big one: 🔴 ~3–5 eng-weeks, **re-scope needed** (re-verified 2026-08-30: 299 files, but the largest block is NSwag-generated and the real cost is three breaking public contracts plus System.Text.Json's strictness on data we did not write; `IJsonProvider` is Newtonsoft-typed at its interface; Umbraco 17 itself keeps Newtonsoft transitively, so the payoff is consistency, not dependency reduction). Phases 0–1 are non-breaking and drop 4 packages on their own. See [`BACKLOG_SCOPING.md`](BACKLOG_SCOPING.md) §1, which also carries the interim mixed-stack boundary rule and the 2026-08-30 boundary-violation sweep.
- **Move remaining UIs to React** — migrate the remaining Lit / legacy backoffice UIs to React.
- **Package upgrades — Mediator (evaluating **Wolverine**).** *Not actually blocked today:* the pinned `MediatR 12.5.0` is the last MIT/Apache version (only a v13+ upgrade triggers the commercial license). Coupling is ~5 files behind the N3O `IMediator` abstraction; Wolverine core is MIT (but a distributed bus, overkill). ⚠️ Not urgent — proactive-upgrade timing is the decision. See [`BACKLOG_SCOPING.md`](BACKLOG_SCOPING.md) §3.
- **Lifetime-scope hacks** — fix the workarounds introduced for DI lifetime scopes (around `HttpContextAccessor` / `IUmbracoContextAccessor`).
- **Cloud project de-duplication** — remove duplication in the `Cloud.Platforms` project.
- **`checkout` / `accounts` etc. deletion in the `v17` branch** — *to discuss* (whether these projects should be dropped in v17).

---

*Generated 2026-06-11. Per-project PRs target `v17`; `v17` itself = a clean `origin/main` base accumulating the migration PRs.*
