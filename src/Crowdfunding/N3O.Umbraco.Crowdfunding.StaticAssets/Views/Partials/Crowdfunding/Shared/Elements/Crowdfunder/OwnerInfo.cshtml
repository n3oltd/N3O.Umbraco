@model ICrowdfunderViewModel

@inject IPartialText PartialText

@{
    PartialText.SetPartialName("CrowdfundingCrowdfunderOwnerInfo");
}

<div class="auto__container">
    <div class="hero__inner cov">
        @if (Model.EditMode()) {
            <div class="campaign__edit modallBtn" n3o-editor="@(Model, PropertyTypes.Cropper, nameof(Model.Content.BackgroundImage))">
                <svg>
                    <use xlink:href="#imagePlus"></use>
                </svg>
                
                <b>@PartialText.Get("Edit Cover Image")</b>
            </div>
        }
        
        <div class="account sm">
            <div class="account__main">
                <div class="account__info">
                    <div class="account__info-avatar">
                        <img src="@Model.OwnerInfo.ProfilePicture" alt="">
                    </div>
                    
                    <div class="account__info-name">
                        <div class="large">
                            @Model.OwnerInfo.Name
                        </div>
                    </div>
                </div>
                
                <div class="account__buttons">
                    <button type="button" class="button secondary modallBtn" data-mod="#modal-1">
                        <svg>
                            <use xlink:href="#shareIcon"></use>
                        </svg>
                        
                        @PartialText.Get("Share")
                    </button>
                </div>
            </div>
        </div>
    </div>
</div>