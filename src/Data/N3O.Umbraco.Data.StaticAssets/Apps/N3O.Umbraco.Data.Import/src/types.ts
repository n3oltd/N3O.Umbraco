export interface ContentType {
    alias: string;
    name: string;
}

export interface DatePattern {
    id: string;
    name: string;
}

export interface ImportableProperty {
    alias: string;
    columnTitle: string;
    selected: boolean;
}
